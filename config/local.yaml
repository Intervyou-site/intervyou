# Local development configuration
database:
  host: localhost
  port: 5432
  name: intervyou_local
  
api:
  host: 0.0.0.0
  port: 8000
  debug: true

ml_models:
  xlnet_model: "xlnet-base-cased"
  huggingface_cache: "./models"
  
logging:
  level: DEBUG
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"