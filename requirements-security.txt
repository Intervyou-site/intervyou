# Security Dependencies
# Input validation and sanitization

# Core validation
pydantic>=2.0.0
bleach>=6.0.0

# File type detection
python-magic>=0.4.27
python-magic-bin>=0.4.14; sys_platform == 'win32'

# Additional security
email-validator>=2.0.0

# Already in main requirements (verify these are present)
# fastapi>=0.100.0
# sqlalchemy>=2.0.0
